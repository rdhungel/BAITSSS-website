name: Split architecture heading
on:
  push:
    branches: [gh-pages]
    paths:
      - '.github/scripts/split-architecture-heading.py'
      - '.github/workflows/split-architecture-heading.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: gh-pages
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/scripts/split-architecture-heading.py
      - run: |
          git config user.name "BAITSSS site maintenance"
          git config user.email "actions@users.noreply.github.com"
          git add animation-evaluation-chip-test.html index.html
          git diff --cached --quiet && exit 0
          git commit -m "Split modernized desktop architecture heading into two lines"
          git push origin gh-pages
